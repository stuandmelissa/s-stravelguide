# Install Order

1. Extract Packs 1–4 into the project root.
2. Extract Pack 5 into the same project root and allow folders to merge.
3. Do not delete prior knowledge or data files.
4. Have the coding agent read every file in `knowledge/` in numeric order.
5. Import `data/park-intelligence.json` as offline seed data.
6. Display `verifiedAt` for volatile park operations and refresh from official sources when connected.
